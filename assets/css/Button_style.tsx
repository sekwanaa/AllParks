import { StyleSheet } from 'react-native'

export const ButtonStyles = StyleSheet.create({
  container: {
    alignItems: 'center',
    justifyContent: 'center',
  },
  border: {
    borderRadius: 40,
    backgroundColor: '#FFF',
    width: '70%'
  }
});

